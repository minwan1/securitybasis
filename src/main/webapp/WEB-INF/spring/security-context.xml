<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">
		
		
		
	
	<security:http auto-config="true" use-expressions="true"> <!-- 앞에 security는 네임스페이스임    expressio은 ip와같이 접근 권한 제한을 좀더풍부하게 할수 있음-->
		<security:intercept-url pattern="/admin/**" access="hasRole(ROLE_ADMIN)" />
		<security:intercept-url pattern="/manager/**" access="hasAuthority(ROLE_USER)" /> <!--hasRole/hasAuthority 같은뜻 저권한 가지만 사람만 접근가 -->
		<security:intercept-url pattern="/member/**" access="isAuthenticated()" /> <!-- 인증된 사람만 접근 가능  -->
		<security:intercept-url pattern="/**" access="permitAll" />
		<security:form-login login-page="/loginForm"/>
		<!-- <security:form-login login-page="/loginForm" authentication-failure-url="성공유알엘 "/> -->
	</security:http>
	
	
	<security:authentication-manager> <!-- 인메모리 사용자 db방벙 --> 
		<security:authentication-provider>
			<security:user-service>
				<security:user name="bkchoi" password="1234" authorities="ROLE_USER"/>
				<security:user name="manager" password="qwer" authorities="ROLE_MANAGER"/>
				<security:user name="admin" password="asdf" authorities="ROLE_ADMIN,ROLE_USER"/>			
			</security:user-service>		
		</security:authentication-provider> 
	</security:authentication-manager>
	


</beans>
